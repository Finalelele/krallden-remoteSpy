-- [[ KRALLDEN SPY v9.2 - STABLE & FIXED ]] --

local player = game:GetService("Players").LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

if playerGui:FindFirstChild("KralldenSpyUI") then playerGui.KralldenSpyUI:Destroy() end

local ScreenGui = Instance.new("ScreenGui", playerGui)
ScreenGui.Name = "KralldenSpyUI"; ScreenGui.ResetOnSpawn = false; ScreenGui.DisplayOrder = 2147483647

local Main = Instance.new("Frame", ScreenGui)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 20); Main.Size = UDim2.new(0, 820, 0, 440)
Main.Position = UDim2.new(0.5, -410, 0.5, -220); Main.Active = true; Main.Draggable = true; Main.BorderSizePixel = 0

local MainMemory, PathFilter, BannedPaths = {}, {}, {}
local AntiSpamCooldowns, AntiSpamCounts = {}, {}
local selfMode, controlMode, antiSpam = true, true, false
local spyFS, spyFC, spyIS = true, false, false
local currentSelectionGUID, lastCount = nil, 0

-- Размеры для сворачивания
local fullSize = UDim2.new(0, 820, 0, 440)
local minSize = UDim2.new(0, 185, 0, 35) -- Ширина 185, чтобы текст не обрезался

local function generateGUID() return tostring(tick()) .. "-" .. tostring(math.random(1, 100000)) end

-- ФУНКЦИЯ ОЧИСТКИ
local function fullClear()
    MainMemory, PathFilter, lastCount, currentSelectionGUID = {}, {}, 0, nil
    BannedPaths, AntiSpamCounts, AntiSpamCooldowns = {}, {}, {}
    local details = Main:FindFirstChild("Details", true)
    if details then details.Text = "" end
    local scroll = Main:FindFirstChild("ScrollingFrame", true)
    if scroll then 
        for _, v in pairs(scroll:GetChildren()) do 
            if v:IsA("TextButton") then v:Destroy() end 
        end 
    end
end

-- HEADER
local Header = Instance.new("Frame", Main)
Header.Size = UDim2.new(1, 0, 0, 35); Header.BackgroundColor3 = Color3.fromRGB(25, 25, 30); Header.ZIndex = 10; Header.BorderSizePixel = 0

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0, 140, 1, 0); Title.BackgroundTransparency = 1; Title.Position = UDim2.new(0, 10, 0, 0)
Title.Text = "KRALLDEN SPY"; Title.TextColor3 = Color3.new(1, 1, 1); Title.Font = Enum.Font.SourceSansBold; Title.TextSize = 16; Title.ZIndex = 11; Title.TextXAlignment = 0

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 40, 0, 35); MinBtn.Position = UDim2.new(1, -40, 0, 0); MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 180); MinBtn.Text = "_"; MinBtn.TextColor3 = Color3.new(1, 1, 1); MinBtn.TextSize = 22; MinBtn.ZIndex = 12; MinBtn.BorderSizePixel = 0

local ControlBtn = Instance.new("TextButton", Header)
ControlBtn.Size = UDim2.new(0, 100, 0, 24); ControlBtn.Position = UDim2.new(1, -145, 0.5, -12); ControlBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 255); ControlBtn.Text = "CONTROL: ON"; ControlBtn.TextColor3 = Color3.new(1,1,1); ControlBtn.Font = Enum.Font.SourceSansBold; ControlBtn.TextSize = 11; ControlBtn.ZIndex = 11

local SelfBtn = Instance.new("TextButton", Header)
SelfBtn.Size = UDim2.new(0, 80, 0, 24); SelfBtn.Position = UDim2.new(1, -230, 0.5, -12); SelfBtn.BackgroundColor3 = Color3.fromRGB(45, 90, 45); SelfBtn.Text = "SELF: ON"; SelfBtn.TextColor3 = Color3.new(1,1,1); SelfBtn.Font = Enum.Font.SourceSansBold; SelfBtn.TextSize = 11; SelfBtn.ZIndex = 11

local AntiSpamBtn = Instance.new("TextButton", Header)
AntiSpamBtn.Size = UDim2.new(0, 90, 0, 24); AntiSpamBtn.Position = UDim2.new(1, -325, 0.5, -12); AntiSpamBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 65); AntiSpamBtn.Text = "ANTI-SPAM: OFF"; AntiSpamBtn.TextColor3 = Color3.new(1,1,1); AntiSpamBtn.Font = Enum.Font.SourceSansBold; AntiSpamBtn.TextSize = 10; AntiSpamBtn.ZIndex = 11; AntiSpamBtn.Visible = false

local ContentFrame = Instance.new("Frame", Main)
ContentFrame.Name = "ContentFrame"; ContentFrame.Size = UDim2.new(1, 0, 1, -35); ContentFrame.Position = UDim2.new(0, 0, 0, 35); ContentFrame.BackgroundTransparency = 1; ContentFrame.ClipsDescendants = true

-- СВОРАЧИВАНИЕ (СМЕЩЕНИЕ ВПРАВО)
MinBtn.MouseButton1Click:Connect(function()
    ContentFrame.Visible = not ContentFrame.Visible
    local isVis = ContentFrame.Visible
    
    ControlBtn.Visible = isVis
    SelfBtn.Visible = isVis
    AntiSpamBtn.Visible = (isVis and not controlMode)
    
    local targetSize = isVis and fullSize or minSize
    local currentPos = Main.Position
    local targetPos
    
    if not isVis then
        -- Сдвигаем вправо на разницу размеров
        targetPos = currentPos + UDim2.new(0, fullSize.X.Offset - minSize.X.Offset, 0, 0)
    else
        -- Возвращаем влево при развороте
        targetPos = currentPos - UDim2.new(0, fullSize.X.Offset - minSize.X.Offset, 0, 0)
    end
    
    Main:TweenSizeAndPosition(targetSize, targetPos, "Out", "Quad", 0.15, true)
    MinBtn.Text = isVis and "_" or "+"
    if isVis then lastCount = -1 end
end)

-- ПАРСЕР И ЛОГИКА (Остальное без изменений из v9.1)
local function parseTable(t, depth)
    depth = depth or 0
    if type(t) ~= "table" or depth > 4 then return type(t) == "string" and '"'..t..'"' or tostring(t) end
    local result, isArray, count, i = "{", true, 0, 0
    for k, v in pairs(t) do count = count + 1; if type(k) ~= "number" then isArray = false end end
    for k, v in pairs(t) do
        i = i + 1; if i > 15 then result = result .. "... " break end
        local val = parseTable(v, depth + 1)
        result = result .. (isArray and val or '["'..tostring(k)..'"] = ' .. val)
        if i < count then result = result .. ", " end
    end
    return result .. "}"
end

local function getSafePath(obj)
    local p = ""
    pcall(function()
        local t = obj
        while t and t ~= game do
            local n = tostring(t.Name); p = (n:match("[%s%W]") and '["'..n..'"]' or n) .. (p ~= "" and "." .. p or ""); t = t.Parent
        end
    end)
    return p ~= "" and "game." .. p:gsub("%.%[", "[") or "game.NilObject"
end

local function addLog(rem, args, isSelf, typeLabel)
    if (typeLabel == "FS" and not spyFS) or (typeLabel == "FC" and not spyFC) or (typeLabel == "IS" and not spyIS) then return end
    local eventPath = getSafePath(rem)
    local argStrings = {}
    for _, v in pairs(args) do pcall(function() table.insert(argStrings, (typeof(v) == "Instance" and getSafePath(v) or parseTable(v))) end) end
    local finalArgs = table.concat(argStrings, ", ")
    local sig = eventPath .. finalArgs
    
    if not controlMode and antiSpam and not isSelf then
        if BannedPaths[eventPath] then return end
        if PathFilter[sig] then return end
        local now = tick()
        if (now - (AntiSpamCooldowns[eventPath] or 0)) < 0.6 then
            AntiSpamCounts[eventPath] = (AntiSpamCounts[eventPath] or 0) + 1
            if AntiSpamCounts[eventPath] >= 3 then BannedPaths[eventPath] = true return end
            return 
        else AntiSpamCounts[eventPath] = 0 end
        AntiSpamCooldowns[eventPath] = now
    end

    local filterSig = controlMode and eventPath or sig
    if not isSelf and PathFilter[filterSig] then return end
    if isSelf and selfMode and PathFilter["S" .. filterSig] then return end
    
    PathFilter[isSelf and "S" .. filterSig or filterSig] = true
    local method = (typeLabel == "IS" and "InvokeServer" or (typeLabel == "FC" and "FireClient" or "FireServer"))
    
    table.insert(MainMemory, 1, {
        guid = generateGUID(), name = tostring(rem.Name), type = typeLabel, 
        fullText = string.format("Type: %s\nPath: %s\n\nArgs: %s\n\nScript: %s:%s(%s)", typeLabel, eventPath, finalArgs, eventPath, method, finalArgs), 
        isSelf = isSelf
    })
end

local mt = getrawmetatable(game); local old = mt.__namecall; setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local m = getnamecallmethod(); local a = {...}; local s = checkcaller()
    if m == "FireServer" or m == "fireServer" then task.spawn(addLog, self, a, s, "FS")
    elseif m == "FireClient" or m == "fireClient" then task.spawn(addLog, self, a, s, "FC")
    elseif m == "InvokeServer" or m == "invokeServer" then task.spawn(addLog, self, a, s, "IS") end
    return old(self, ...)
end)
setreadonly(mt, true)

-- UI BUTTONS & LOOP
local Scroll = Main:FindFirstChild("ScrollingFrame", true)
local Details = Main:FindFirstChild("Details", true)

ControlBtn.MouseButton1Click:Connect(function() 
    controlMode = not controlMode; fullClear()
    ControlBtn.Text = "CONTROL: "..(controlMode and "ON" or "OFF")
    ControlBtn.BackgroundColor3 = controlMode and Color3.fromRGB(150, 50, 255) or Color3.fromRGB(80, 80, 85)
    AntiSpamBtn.Visible = not controlMode
end)

AntiSpamBtn.MouseButton1Click:Connect(function()
    antiSpam = not antiSpam
    AntiSpamBtn.Text = "ANTI-SPAM: "..(antiSpam and "ON" or "OFF")
    AntiSpamBtn.BackgroundColor3 = antiSpam and Color3.fromRGB(180, 150, 40) or Color3.fromRGB(60, 60, 65)
end)

SelfBtn.MouseButton1Click:Connect(function() selfMode = not selfMode; SelfBtn.Text = "SELF: "..(selfMode and "ON" or "OFF"); SelfBtn.BackgroundColor3 = selfMode and Color3.fromRGB(45, 90, 45) or Color3.fromRGB(150, 50, 50) end)

task.spawn(function()
    while task.wait(0.5) do
        if not ContentFrame.Visible or #MainMemory == lastCount then continue end
        lastCount = #MainMemory
        for _, child in pairs(Scroll:GetChildren()) do if child:IsA("TextButton") then child:Destroy() end end
        for i, data in ipairs(MainMemory) do
            local b = Instance.new("TextButton", Scroll)
            b.Size = UDim2.new(1, -6, 0, 30); b.LayoutOrder = i; b.Text = string.format("[%s]%s %s", data.type, (data.isSelf and " [S]" or ""), data.name)
            b.BackgroundColor3 = (currentSelectionGUID == data.guid) and Color3.fromRGB(150, 50, 255) or (data.isSelf and Color3.fromRGB(35, 70, 35) or Color3.fromRGB(40, 40, 45))
            b.TextColor3 = Color3.new(1,1,1); b.BorderSizePixel = 0
            b.MouseButton1Click:Connect(function()
                currentSelectionGUID = data.guid; Details.Text = data.fullText
                for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v.BackgroundColor3 = (v.Text:find("%[S%]") and Color3.fromRGB(35, 70, 35) or Color3.fromRGB(40, 40, 45)) end end
                b.BackgroundColor3 = Color3.fromRGB(150, 50, 255)
            end)
        end
    end
end)
